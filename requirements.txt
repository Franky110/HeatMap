# Packages needed by combine_trips.py
# (The Tkinter GUI, trip_manager.py, only needs the standard library + tkinter.)
# combine_trips.py itself only uses the standard library — no extra packages needed.

# Used by the Strava import window.
requests==2.32.3
